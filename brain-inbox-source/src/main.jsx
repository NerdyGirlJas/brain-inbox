import React from "react";
import ReactDOM from "react-dom/client";
import RootSystem from "./RootSystem.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RootSystem />
  </React.StrictMode>
);
